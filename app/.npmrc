# This project installs with `deno install` (see README). These settings only
# matter if someone runs npm here anyway: npm ignores deno.lock, so at least
# never run package install scripts and never save version ranges.
ignore-scripts=true
save-exact=true

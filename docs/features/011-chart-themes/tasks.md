# 011. Chart themes: tasks

- [x] Theme list and support detection from Flint
- [x] Vega-Lite and Plotly overlays keep Flint's styling when themed; App
      unchanged
- [x] The theme passed to ECharts and Chart.js once Flint supports them
- [x] Every axis tick labelled, whatever a theme's tick count
- [x] `chartTheme` setting and the Chart theme dropdown, noting libraries that
      can't show a theme
- [x] Themed Vega-Lite bands kept inside the plot, so a theme's axis lines show
- [x] Each theme checked in cards and the large view, light and dark
- [x] ADR 0016 and the steps for updating Flint or a chart library

# 004. Desktop shell and bindings: tasks

- [x] Spikes: Deno-only Vue bundling, Vite under Deno in a desktop window,
      SQLite across restarts
- [x] Workspace and tasks; exact pins, frozen lockfile, 7-day minimum age,
      `.npmrc`
- [x] Claude Design canvas: welcome, dashboard, zoomed test, one-report state
      (fictional data)
- [x] `desktop/main.ts`: window, database, launch upgrade, bindings, serve the
      build
- [x] `DesktopBindings` contract and handlers with argument checks
- [x] Window explains a database that won't open
- [x] Fake bindings and development-only loading in the browser
- [x] Contract suite against both implementations
- [x] Fictional sample generator and samples
- [x] Theme toggle (system, light, dark), stored in settings
- [x] Icon folder, `Icon` component and icon rules test
- [x] README and `docs/development.md`: getting started, the two ways to run,
      supply-chain notes
